#inlude <vector>
using namespace std;


class Solution {
public:
    int minOperations(vector<vector<int>>& grid, int x) {
        vector<int> cnt;
        for(vector<vector<int>>::iterator it1 : grid.begin())
        {
            for (vector<int>::iterator it2 : (*it1).begin())
            {
                
            }
        }
    }
};